spring:
  datasource:
    url: ${TEST_DATABASE_URL:jdbc:postgresql://127.0.0.1:55432/pelada_test}
    username: ${TEST_DB_USER:pelada}
    password: ${TEST_DB_PASSWORD:pelada}
app:
  demo-enabled: false
